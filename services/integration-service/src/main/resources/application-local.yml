spring:
  config:
    activate:
      on-profile: local
  datasource:
    url: jdbc:postgresql://127.0.0.1:5433/plp_db?currentSchema=plp_integration
  rabbitmq:
    port: 5673

server:
  port: 8184

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8861/eureka/
